# Copy to config.ini and customize with your values.

[Settings]
RMQ_HOST = <host of the RabbitMQ server>

# Bearer token for the BCTM OPS telemetry API
ACCESS_TOKEN = <your Bearer token>

# Base URL for the BCTM OPS telemetry API (optional - defaults to the value below)
# TLM_API_URL = https://api.bctmops.com/tlm
